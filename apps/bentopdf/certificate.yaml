apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bentopdf-tls
  namespace: bentopdf
spec:
  secretName: bentopdf-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${BENTOPDF_HOST}
