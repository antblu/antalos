apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: authentik-ldap
  namespace: authentik
spec:
  entryPoints:
    - ldaps
  routes:
    - match: HostSNI(`${LDAP_HOST}`)
      services:
        - name: ak-outpost-ldap-outpost
          port: 389
  tls:
    secretName: ldap-tls
