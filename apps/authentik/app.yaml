apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: authentik
  namespace: argocd
spec:
  project: default

  sources:
    - repoURL: https://charts.goauthentik.io
      chart: authentik
      targetRevision: ${AUTHENTIK_CHART_VERSION}
      helm:
        values: |
          global:
            affinity:
              podAntiAffinity: hard
            volumeMounts:
              - name: media
                mountPath: /data
            volumes:
              - name: media
                persistentVolumeClaim:
                  claimName: authentik-media
            env:
              - name: AUTHENTIK_POSTGRESQL__HOST
                value: authentik-db-rw.authentik.svc.cluster.local
              - name: AUTHENTIK_POSTGRESQL__PASSWORD
                valueFrom:
                  secretKeyRef:
                    name: authentik-db-app
                    key: password
              - name: AUTHENTIK_EMAIL__HOST
                value: "${AUTHENTIK_EMAIL_HOST}"
              - name: AUTHENTIK_EMAIL__PORT
                value: "${AUTHENTIK_EMAIL_PORT}"
              - name: AUTHENTIK_EMAIL__USERNAME
                value: "${AUTHENTIK_EMAIL_USERNAME}"
              - name: AUTHENTIK_EMAIL__PASSWORD
                valueFrom:
                  secretKeyRef:
                    name: authentik-email
                    key: password
              - name: AUTHENTIK_EMAIL__USE_TLS
                value: "${AUTHENTIK_EMAIL_USE_TLS}"
              - name: AUTHENTIK_EMAIL__USE_SSL
                value: "${AUTHENTIK_EMAIL_USE_SSL}"
              - name: AUTHENTIK_EMAIL__FROM
                value: "${AUTHENTIK_EMAIL_FROM}"
              # Increment when authentik-email changes so pods reload secret-backed env.
              - name: EMAIL_SECRET_REVISION
                value: "${AUTHENTIK_EMAIL_SECRET_REVISION}"

          authentik:
            existingSecret:
              secretName: authentik-secrets

          server:
            replicas: 2
            deploymentStrategy:
              type: RollingUpdate
              rollingUpdate:
                maxSurge: 0
                maxUnavailable: 1

            metrics:
              enabled: true
              service:
                labels:
                  metrics.antblu.net/scrape: authentik-server

            ingress:
              enabled: true
              ingressClassName: traefik
              hosts:
                - ${AUTHENTIK_HOST}
              tls:
                - hosts:
                    - ${AUTHENTIK_HOST}
                  secretName: authentik-tls

          worker:
            replicas: 2
            deploymentStrategy:
              type: RollingUpdate
              rollingUpdate:
                maxSurge: 0
                maxUnavailable: 1
            metrics:
              enabled: true
              service:
                labels:
                  metrics.antblu.net/scrape: authentik-worker

          postgresql:
            enabled: false

    - repoURL: https://github.com/antblu/antalos.git
      targetRevision: main
      path: apps/authentik
      plugin:
        name: yaml-envsubst

  destination:
    server: https://kubernetes.default.svc
    namespace: authentik

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
