apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: node-red-tls
  namespace: node-red
spec:
  secretName: node-red-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${NODE_RED_HOST}
