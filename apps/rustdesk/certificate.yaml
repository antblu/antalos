apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rustdesk-tls
  namespace: rustdesk
spec:
  secretName: rustdesk-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${RUSTDESK_HOST}
