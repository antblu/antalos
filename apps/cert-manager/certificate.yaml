apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: router-tls
  namespace: cert-manager
spec:
  secretName: router-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${ROUTER_HOST}
