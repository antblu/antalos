apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: urbackup-tls
  namespace: urbackup
spec:
  secretName: urbackup-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${URBACKUP_HOST}
