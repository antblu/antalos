apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zammad-tls
  namespace: zammad
spec:
  secretName: zammad-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${ZAMMAD_HOST}
